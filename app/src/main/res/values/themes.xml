<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CoinRich" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Customize the navigation color -->
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <!-- Customize the top color -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
    </style>

    <style name="Theme.CoinRich" parent="Base.Theme.CoinRich" />

    <style name="Theme.CoinRich.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.CoinRich.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.CoinRich.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>